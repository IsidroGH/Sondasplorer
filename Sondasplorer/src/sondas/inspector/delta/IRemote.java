package sondas.inspector.delta;

/**
 * Indica que corresponde a un elemento remoto, por ejemplo otro nodo.
 * Se utiliza por ejemplo para la vista de Mapa donde obtengo todos los global remotos
 * a partir de los cuales obtengo las máquinas donde están siendo invocados y sus metricas
 */
public interface IRemote {
	
}
